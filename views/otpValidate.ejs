<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Enter OTP</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">

    <style>
        /* General Styles */
        body {
            background-color: #F9E642;
        }

        /* Colors */
        .bg-maroon {
            background-color: #a05252;
        }

        .text-maroon {
            color: #a05252;
        }

        .btn-maroon {
            background-color: #a05252;
            color: #fff;
            border: none;
        }

        .btn-maroon:hover {
            background-color: #904545;
        }

        .bg-light-gray {
            background-color: #f8f9fa;
        }

        /* Card Styling */
        .card {
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        }

        .card-header {
            background-color: #a05252;
            color: white;
        }

        .card-footer a {
            color: #a05252;
        }

        .card-footer a:hover {
            color: #904545;
        }
    </style>
</head>

<body>

    <div class="container">
        <div class="row justify-content-center">
            <div class="col-md-4">
                <div class="card mt-5">
                    <div class="card-header text-center text-light bg-maroon">
                        <h3>Enter OTP</h3>
                    </div>
                    <div class="card-body bg-light-gray">
                        <form action="/checkOtpController/<%= id %>" method="POST">
                            <!-- OTP Field -->
                            <div class="mb-3">
                                <label for="otp" class="form-label text-maroon">Enter OTP</label>
                                <input type="text" class="form-control" id="otp" placeholder="Enter OTP" name="otp">
                            </div>
                            
                            <div class="d-grid">
                                <button type="submit" class="btn btn-maroon">Verify OTP</button>
                            </div>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>
